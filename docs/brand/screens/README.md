# Screen gallery (Sprint 6 sign-off)

Generate on any machine with normal internet (the CI sandbox blocks
Expo's dev-server API):

    npm run demo   # expo web with canned data
    # open http://localhost:8081 at 390x844 (iPhone 14) and capture:
    # home, agenda, connect, more, session detail, speaker detail,
    # attendee profile, auction, questions, welcome (moment/dark)

Commit captures here as <screen>.png for Kylie's sign-off.
